# context-kit

Context hygiene kit for a single AI agent — pruning, scratch persistence, delegation-brief validation, safety hooks, memory recall.

> **Status: WIP (private).** Scaffold only — equipment lands one PR at a time. Full README follows the readme-craft process before public flip. See issue #1 for the kickoff plan.

## Layout (planned)

- `bin/` — proactive CLI tools (`lg`, `recall`)
- `hooks/` — Claude Code hook scripts (scratch-persist, brief validator, safety hooks)
- `examples/` — `settings.json` wiring examples
- `docs/` — setup and design notes
